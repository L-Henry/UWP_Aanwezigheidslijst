<Page
    x:Class="UWP_Aanwezigheidslijst.UWP.View.CreateDocentView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWP_Aanwezigheidslijst.UWP.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="8*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.ColumnSpan="2" Grid.Row="0" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="30">Docent Aanmaken</TextBlock>
        <TextBox Grid.Column="0" Grid.Row="1" PlaceholderText="Naam" Margin="30" Text="{x:Bind ViewModel.Naam, Mode=TwoWay}"></TextBox>
        <TextBox Grid.Column="0" Grid.Row="2" PlaceholderText="Bedrijf" Margin="30" Text="{x:Bind ViewModel.Bedrijf, Mode=TwoWay}"></TextBox>
        <Button Grid.Column="1" Grid.Row="3" Command="{x:Bind ViewModel.SaveCommand}">Save</Button>
        
    </Grid>
</Page>
